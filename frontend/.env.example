# Local dev talks to http://localhost:4110 automatically — nothing needed here.
# For a split deploy (frontend separate from backend), point at the backend URL:
# VITE_API_URL=https://your-app.fly.dev
